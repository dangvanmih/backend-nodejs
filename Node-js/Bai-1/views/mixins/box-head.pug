//- cách dưới đây giống với các tạo 1 hàm function trong react rồi sau đó nhận tham số truyền vào
mixin box-head(title, description)
  div(class = "box-head")
    h1(class = "inner-title") #{title}
    div(class = "inner-desc") #{description}