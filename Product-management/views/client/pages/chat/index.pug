extends ../../layouts/default.pug

block css
  link(rel="stylesheet" href="/css/chat.css")

block main
  .container.my-5
    .row.justify-content-center
      .col-md-10.col-lg-8
        .chat(my-id=user.id my-name=user.fullName).chat-card.card.shadow-lg.border-0
          //- Header của khung chat
          .card-header.bg-white.py-3.border-bottom
            .d-flex.align-items-center
              .position-relative
                img(src="/images/logo-tab.png" class="rounded-circle mr-3" width="45" height="45")
                span.status-online
              div
                h6.mb-0.font-weight-bold Nhóm Chat Cộng Đồng
                small.text-success Đang hoạt động

          //- Vùng hiển thị tin nhắn
          .inner-body.card-body.chat-box#chat-box(data-lenis-prevent)

            each chat in chats
              div(class=(chat.infoUser._id == user.id ? "inner-outgoing" :  "inner-incoming"))
                if(chat.infoUser.id != user.id)
                  .inner-name #{chat.infoUser.fullName}
                if(chat.content)
                  .inner-content
                    | #{chat.content}
                if(chat.images)
                  .inner-images
                    each image in chat.images 
                      img(src=image)
                .inner-time #{moment(chat.createdAt).format("hh:mm A")}

            div(class="inner-list-typing")
          
          //- Thanh nhập liệu
          .card-footer.bg-white.p-3
            .inner-preview-images 
              div(class="custom-file-container" data-upload-id="upload-image")

            form.inner-form#form-chat(action="")
              .input-group
                
                input(
                  type="text"
                  class="form-control   bg-light shadow-none"
                  placeholder="Nhập tin nhắn..."
                  name="content"
                  autocomplete="off"
                )
                .input-group-prepend
                  span.button-icon.btn.btn-light
                    i.fa-regular.fa-face-smile
                  div(class="tooltip" role="tooltip")
                    emoji-picker
                label(class="btn btn-light mb-0" for="file-upload-with-preview-upload-image") 
                  <i class="fa-regular fa-image"></i>
                .input-group-append
                  button.btn.btn-primary.px-4(type="submit")
                    i.fa-solid.fa-paper-plane
